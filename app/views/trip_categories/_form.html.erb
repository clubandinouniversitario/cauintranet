<%= form_with(model: trip_category, local: true) do |form| %>
  <% if trip_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(trip_category.errors.count, "error") %> prohibited this trip_category from being saved:</h2>

      <ul>
        <% trip_category.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
